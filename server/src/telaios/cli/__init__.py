"""
telaios.cli
-----------
Developer TUI for evaluating TelaiOS RAG and agent capabilities.

Run via:
    uv run telaios-eval

Requires the ``agents`` optional-dependency group to be installed for live
LLM calls.  Works in dry-run (mock) mode without any external services.
"""
