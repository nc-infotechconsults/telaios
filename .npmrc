# Force flat hoisting of shared packages to root node_modules
install-strategy=hoisted
# Ignore peer dependency conflicts (express-ws@5 declares a peer range that conflicts at root)
legacy-peer-deps=true
